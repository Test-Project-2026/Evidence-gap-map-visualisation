# Connection string for Prisma CLI (migrate/generate) and runtime
# Uses the same DATABASE_URL env var for both (Prisma 7 reads from prisma.config.ts)
DATABASE_URL="postgresql://user:password@host:5432/db?sslmode=require"
